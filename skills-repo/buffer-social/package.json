{
  "name": "buffer-skill",
  "version": "1.0.0",
  "description": "CLI tool and OpenClaw skill for Buffer GraphQL API",
  "type": "module",
  "bin": {
    "buffer": "./buffer.js"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "axios": "^1.6.0",
    "chalk": "^5.3.0",
    "commander": "^12.0.0",
    "dotenv": "^16.4.0",
    "ora": "^8.0.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^1.6.1",
    "prettier": "^3.8.1",
    "vitest": "^1.3.0"
  }
}
